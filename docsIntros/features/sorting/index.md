# Sorting

The sorting feature enables ordered row rendering.

It supports:
- single-field sorting
- direction control
- custom compare functions

Sorting logic is independent of UI.
Sort controls are provided via the SortBar slot.
