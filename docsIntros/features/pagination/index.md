# Pagination

The pagination feature manages page slicing and metadata.

It provides:
- page index and page size
- total item and page counts
- derived visible rows

Pagination is purely logical.
Rendering controls is the responsibility of the Pagination slot.
