# Filtering

The filtering feature provides client-side filtering support.

It manages:
- filter state
- field-level filter configuration
- normalization and operators
- derived row visibility

The feature does not impose any UI.
Filter controls must be provided via a FiltersPanel slot.
