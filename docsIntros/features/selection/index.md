# Selection

The selection feature manages row selection state.

It supports:
- none / single / multiple selection modes
- toggle and bulk selection helpers
- integration with actions

Selection logic is independent of UI.
Checkboxes or indicators must be implemented by the Table slot.
